apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: cs2-analytics
  labels:
    app: backend
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
